import { TouchableNativeFeedback } from 'react-native'

export const primaryColor = '#F57C00'

export const lightPrimary = '#FF6F00'

export const accentColor = '#4DB6AC'

export const textColor = 'rgba(0, 0, 0, 0.541176)'

export const nativeRippleColor = TouchableNativeFeedback.Ripple('rgba(255, 255, 255, 0.011176', true)